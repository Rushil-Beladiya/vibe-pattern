import React, { FC } from "react";
import { StyleSheet, Text, View } from "react-native";

interface AdminMusicScreenProps {}

export const AdminMusicScreen: FC<AdminMusicScreenProps> = ({}) => {
  return (
    <View>
      <Text>AdminMusicScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
